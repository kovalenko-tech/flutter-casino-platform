/// Game category taxonomy used for filtering the game catalogue.
///
/// Display labels are handled in the presentation layer via
/// [GameCategoryL10n] extension to support localization.
enum GameCategory {
  all,
  slots,
  live,
  table,
  jackpot;
}
